query RunInput {
  discountNode {
    metafield(namespace: "$app:quantity-discount", key: "function-configuration") {
      value
    }
  }
  cart {
    lines {
      id
      quantity
      merchandise {
        __typename
        ... on ProductVariant {
          id
          product {
            id
          }
        }
      }
    }
  }
}
